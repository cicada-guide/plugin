{
  "name": "cicada-guide-plugin",
  "version": "0.1.0",
  "description": "Plugin for using cicada.guide with Claude (via MCP) and ChatGPT (via an OpenAPI/plugin manifest).",
  "license": "MIT",
  "main": "src/client.js",
  "bin": {
    "cicada-guide-mcp": "src/mcp/server.js"
  },
  "files": [
    "src",
    "openapi.yaml",
    ".well-known"
  ],
  "scripts": {
    "test": "node --test test/*.test.js",
    "start:mcp": "node src/mcp/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.1"
  }
}
